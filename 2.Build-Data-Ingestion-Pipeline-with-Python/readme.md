# python -m venv venv = create python virtual environment named "venv"

# source venv/bin/activate = activate virtual environment

# .\venv\Scripts\Activate.ps1 = activate virtual environment in powershell (windows)

# source venv/Scripts/activate = activate virtual environment in gitbash (windows)

# pip install langchain langchain-community langchain-text-splitters langchain-openai langchain-chroma python-dotenv
