# source documents -> chunking -> (extracted-chunks) -> embedding-model -> (vector-embeddings) -> vectorDB

# source documents = the document.

# chunking = is the process of breaking down bigger documents into smaller chunks.

# embedding model = converts english passages/words = into its associated vector representation or mathematical representation.

# vectorDB = all vector embeddings extracted from embedding model is stored in vector database.