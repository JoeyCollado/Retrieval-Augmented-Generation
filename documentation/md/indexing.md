# Indexing

# fist aspect of indexing is that we have some external documents that we actually want to load and put what we're trying to call "Retriever"

# Retriever = is simply given an input question.

# vectors = used to establish relationship, easier to read than numbers documents are split and each document is compressed into a vector and the vector captures the "Semantic" meaning of the documents itself.