# Rag = "Retrieval-Augmented Generation". It is a technique used to improve artificial intelligence models—specifically large language models (LLMs)—by letting them search an outside database or set of documents for factual information before they write an answer. Think of it like taking an open-book test instead of relying only on memory

# used to increasingly feed LLM's information from external sources. 

# steps :

# query translation = bunch of different methods to take question from user and modify it in some way to make it better suited for retrieval.
#                     it can also use methods like query writing.

# routing = getting the question to the right source

# query construction = taking natural language and converting it into  #                      the DSL necessary for any data source to work 
#                      with.

# indexing = process of taking your documents and processing them in #            some way so they can be easily retrieved
